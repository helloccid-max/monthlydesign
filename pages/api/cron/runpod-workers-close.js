/* 닫는 시각(24:00 KST = 15:00 UTC) 전용 진입점.
 *
 * Vercel은 크론을 경로로 식별해서, 같은 경로에 스케줄만 다르게 두 개를 걸면
 * 하나로 합쳐진다. 여는 시각과 닫는 시각이 각각 걸리도록 경로를 나눈다.
 * 핸들러는 같은 것을 쓴다 — 적용할 값은 어느 스케줄이 울렸는지가 아니라
 * 지금 시각에서 계산하므로, 두 경로의 동작은 완전히 같다. */
export { default } from './runpod-workers';
